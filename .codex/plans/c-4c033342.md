# Codex plan for mehrangta/telegram_project_manager#15

Job: `c-4c033342` · Revision: 1

Rewrite only `README.md` as a concise operator-focused entry point. Reduce the current 310-line, 1,886-word document to no more than 160 lines and 900 words while preserving the minimum setup path, primary Telegram workflows, deployment prerequisites, and the prominent warning that Codex jobs have unrestricted filesystem and network access. Runtime behavior, commands, configuration, schemas, and tests remain unchanged.

## Steps

1. **Restructure the document** — Replace the accumulated feature-by-feature structure with this fixed order: project purpose, requirements, quick start, core commands, workflow summary, safety, and advanced operation. Keep each fact in one section only, use short paragraphs or bullets, and remove implementation history and repeated limits.
   - Likely files: `README.md`
2. **Make quick start self-contained** — Retain the executable onboarding sequence: `uv sync`, database initialization, initial admin registration, creation of `data/secrets.json` containing only `TELEGRAM_BOT_TOKEN`, GitHub CLI authentication, required OpenAI/Codex model and API-key configuration, and starting the bot. Distinguish required settings from optional custom base URLs and the legacy `codex_model` fallback without reproducing the complete configuration-key inventory.
   - Likely files: `README.md`, `src/telegram_project_manager/main.py`, `src/telegram_project_manager/platform/config.py`, `src/telegram_project_manager/platform/secrets.py`
3. **Condense command reference** — Replace the exhaustive command matrix with grouped examples for repository setup, issues and commits, `/code`, `/ask`, `/do`, merge/deploy, configuration, memory, and administration. Preserve exact command spellings and identifiers, state that admin-only operations require a registered admin, and direct users to `/help` for the complete Telegram reference and `telegram-project-manager --help` for CLI syntax.
   - Likely files: `README.md`, `src/telegram_project_manager/bots/commit_manager/commands.py`, `src/telegram_project_manager/bots/code_manager/commands.py`, `src/telegram_project_manager/bots/do_manager/commands.py`, `src/telegram_project_manager/main.py`
4. **Summarize workflows and limits** — Collapse the existing workflow narratives into compact bullets covering topic-scoped repository context, the issue-to-code-to-ready-PR lifecycle, image support, durable `/do` jobs, and merge versus deployment behavior. Retain only user-actionable constraints such as repository allowlisting, approval requirements, deployment being disabled by default, deployment requiring a ready `main` PR and configured `workflow_dispatch` workflow, and interrupted jobs requiring explicit recovery where applicable. Remove internal implementation details such as polling intervals, recovery-turn counts, queue sizes, timing breakdowns, and repeated storage descriptions unless required to operate the feature safely.
   - Likely files: `README.md`, `src/telegram_project_manager/bots/code_manager/service.py`, `src/telegram_project_manager/bots/do_manager/service.py`, `src/telegram_project_manager/bots/pull_request_manager/service.py`
5. **Preserve critical safety messaging** — Keep a short, prominent warning that `/ask`, `/code`, and `/do` run with full host filesystem access and unrestricted outbound network access; prompt restrictions are not sandbox-enforced. Retain concise bullets for repository allowlisting, isolated worktrees, secret redaction, protected paths, the 20 MB code-job change limit, and host-owned GitHub writes. Keep the existing OpenAI security guidance link, but remove duplicated full-access explanations from individual workflow sections.
   - Likely files: `README.md`
6. **Trim advanced operations** — Merge the managed-cache and service sections into a compact advanced-operation section. Recommend `/repo setup owner/repository`, retain the validation requirements for manually supplied caches and `/repo local set`, but remove the long mirror-clone bootstrap example. Mention the separate `run-do-worker` process and tracked systemd unit for durable `/do` jobs, and replace generic systemctl/configuration command dumps with pointers to CLI help.
   - Likely files: `README.md`, `deploy/telegram-project-manager-do-worker.service`, `src/telegram_project_manager/main.py`, `src/telegram_project_manager/bots/commit_manager/repository_setup.py`
7. **Review compatibility and rendering** — Confirm that the rewrite changes documentation only, introduces no new behavior, and does not rename commands, settings, files, limits, or lifecycle states. Normalize Markdown fences and heading hierarchy, ensure examples are copyable, verify every retained link and path, and check that essential setup, deployment, and safety facts remain discoverable after removing duplication.
   - Likely files: `README.md`

## Validation

- Run `git diff --check` to detect trailing whitespace or malformed patch formatting.
- Run `wc -l -w README.md` and require no more than 160 lines and 900 words, representing at least a 50% reduction from the current document.
- Run `rg -n '^#{1,6} ' README.md` and manually verify a single, sequential heading hierarchy with no empty or duplicate sections.
- Run `uv run telegram-project-manager --help` and `uv run telegram-project-manager config set --help`, then compare every retained CLI example and configuration key with the generated interface.
- Compare the condensed Telegram command examples with `CommitManager.help()` and the specialized `/code` and `/do` command parsers so no documented syntax is invented or made stale.
- Run `uv run python -m unittest discover -s tests` to confirm the documentation-only change does not accompany accidental source changes. The current planning environment lacks the `uv` executable, so these `uv`-based checks require the repository prerequisite to be installed first.
- Render or preview `README.md` in a GitHub-compatible Markdown viewer and verify code fences, command tables or lists, inline paths, and the external safety link display correctly.

## Risks

- Over-aggressive shortening could remove required bootstrap steps; mitigate by treating token setup, GitHub authentication, model configuration, database initialization, admin creation, and process startup as mandatory quick-start content.
- Removing detailed workflow prose could obscure elevated execution risk; mitigate by placing the unrestricted filesystem/network warning before or immediately after the core workflow summary rather than burying it in advanced operations.
- A shortened command list could be mistaken for the complete interface; label it as common commands and explicitly point to `/help` and CLI `--help` as authoritative references.
- Commands and operational limits have changed frequently in recent README history, so retaining duplicated exhaustive details would recreate drift; keep volatile implementation details out of the landing page and verify all retained facts against source.
- The tracked repository includes only the `/do` worker systemd unit, while the current README lists generic main-service commands; avoid implying that an untracked service unit is provided.
- The absence of a Markdown linter or link checker means structural and rendering validation remains partly manual.
